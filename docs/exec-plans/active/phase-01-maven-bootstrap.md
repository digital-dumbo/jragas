# Phase 01 - Maven and Spring Boot Bootstrap

## Objective
Stand up a buildable Java multi-module skeleton with a Spring Boot API starter.

## Tasks
- [x] Create parent aggregator POM and module list.
- [x] Add Java/tooling plugin management (compiler, surefire, failsafe, jacoco).
- [x] Create `ragas-api` Spring Boot starter app.
- [ ] Ensure `mvn verify` passes at parent level. (Blocked: `mvn` not available in current environment)

## Exit criteria
- Parent build passes locally and in CI.
